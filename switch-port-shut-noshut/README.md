#### The purpose of this script to analyze the status of the `INPUT_PORT` from `config.ini` file and execute command for the `OUTPUT_PORT`.
#### Just edit `config.ini` file for the credentials and ports.
